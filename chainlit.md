# SQL Agent Template

Ask questions about your database in natural language. The assistant will inspect only allowlisted tables, generate a read-only SQL query, execute it, and answer using the returned data plus any schema documentation you provide.

To customize this welcome screen, edit `chainlit.md`.
